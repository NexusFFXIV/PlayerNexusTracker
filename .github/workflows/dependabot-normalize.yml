name: Dependabot normalize NuGet updates

on:
  pull_request_target:
    types: [opened, reopened, synchronize]

permissions:
  contents: write
  pull-requests: write

jobs:
  normalize:
    uses: NexusFFXIV/.github/.github/workflows/dependabot-normalize.yml@main
    with:
      solution: PlayerNexusTracker.sln
    secrets: inherit
